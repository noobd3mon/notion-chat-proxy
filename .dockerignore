# Keep the image small + keep secrets out of the build context.
node_modules
.git
.wrangler
.dev.vars
_research
.superpowers
test
docs
*.md
.gitignore
.dev.vars.example
vitest.config.mjs
wrangler.toml
wrangler.test.toml
# Runtime state (file-backed KV) — must not be baked into the image; the container
# creates it at runtime (ideally on a mounted volume).
data
